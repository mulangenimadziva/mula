# Consultancy Company - Static React Site

Quick local setup:

1. Install dependencies

```bash
cd "C:/Users/USER/Desktop/consultancy company"
npm install
```

2. Run development server

```bash
npm run dev
```

The site is a minimal React app (Vite). The main source is in `src/` and the booking form logs submissions to the console.
